# Generate all possible sorted arrays from alternate elements of two given sorted arrays
# the idea is to use the recursive function, a flag is passed to indicate whether current element
# in output should be taken from a or b.

def generate(a,b,n,m):

    

a = [10,15,25]
b = [1,5,20,30]
n = len(a)
m = len(b)
generate(a,b,n,m)
